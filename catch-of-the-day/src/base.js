import Rebase from 're-base';

//this file is ignore by git for privacy reasons.
import FirebaseLoginData from "../FirebaseLoginData";

const base = Rebase.createClass(FirebaseLoginData);

export default base;